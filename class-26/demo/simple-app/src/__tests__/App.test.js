import App from '../App';

describe('Testing all the app components', () =>  {
  test('Header, footer, and list all display', () => {
    // does the header show up
    // does the footer had a year??
  });
})
